package de.vodafone.tests.PYP.ST;

import de.vodafone.pages.PYP.*;
import io.qameta.allure.Epic;
import io.qameta.allure.Feature;
import io.qameta.allure.Story;
import io.qameta.allure.TmsLink;
import org.testng.annotations.Test;

/*@Epic("PYP")
@Feature("ST")
@Story("WFAT-9836")
public class WFAT_9836 extends PYPSTSetup {
    @TmsLink("WFAT-11715")
    @Test(description = " GC01 - Consumer user - Not paired yet - Verify the pairing flow || FF")
    public void GC01() {
        new PYPLandingPage(driver).GoTologin().GoToBTN();
        new PYPLogin(driver).typeMSISDNandTAN(MSISDN, TAN).clickLoginButton();
        new ConsumerOverview(driver).clickPairConsumerButton();
        new ConsumerPairConfirmation(driver).successNotificationValidation();
        new ConsumerPairConfirmation(driver).notificationValidation();
        new ConsumerPairConfirmation(driver).pageHeadlineValidation();

    }
    @TmsLink("WFAT-11716")
    @Test(description ="GC02 - Enterprise user - Not paired yet - Verify the pairing flow || FF")
    public void GC02() {
        new PYPLandingPage(driver).GoTologin().GoToBTN();
        new PYPLogin(driver).typeMSISDNandTAN(MSISDN, TAN).clickLoginButton();
        new EnterpriseOverview(driver).clickPairEnterpriseButton();
        new EnterprisePairConfirmation(driver).successNotificationValidation();
        new EnterprisePairConfirmation(driver).notificationValidation();
        new EnterprisePairConfirmation(driver).pageHeadlineValidation();
    }
}
*/