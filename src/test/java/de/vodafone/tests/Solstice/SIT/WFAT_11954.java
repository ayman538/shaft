package de.vodafone.tests.Solstice.SIT;

/*import com.shaft.gui.element.ElementActions;
import com.shaft.validation.Validations;
import de.vodafone.pages.Solstice.SolsticeDashboard;
import de.vodafone.pages.Solstice.SolsticeOtherAdmins;
import io.qameta.allure.Description;
import io.qameta.allure.Epic;
import io.qameta.allure.Feature;
import org.testng.annotations.Test;

@Epic("Solstice")
@Feature("SIT")
@Story("WFAT_11954")*/
public class WFAT_11954 extends SolsticeSetup {

   /* @Test(groups = {"WFAT_11954", "regression"})
    @Description(" Given that I'm a logged in Solstice user  ,When I open other admins accordion," +
            "Then  i want to Verify the Solstice MBO - 0 Products")
    private void GC01() {

        new SolsticeDashboard(driver)
                .chooseÜbersichtFromMeinKonto()
                .chooseOtherAdminAccordion();

        (new ElementActions(driver)).click(SolsticeOtherAdmins.moreDetails);

        Validations.assertThat()
                .element(driver.getDriver(), SolsticeOtherAdmins.ProdHeader)
                .text()
                .contains(SolsticeOtherAdmins.ProdHeader_text)
                .withCustomReportMessage("Check that Solstice prod header exist")
                .perform();

        Validations.assertThat()
                .element(driver.getDriver(), SolsticeOtherAdmins.ProdDes)
                .text()
                .contains(SolsticeOtherAdmins.ProdDes_text)
                .withCustomReportMessage("Check that Solstice prod Description exist")
                .perform();

        Validations.assertThat()
                .element(driver.getDriver(), SolsticeOtherAdmins.lessDetails)
                .text()
                .contains(SolsticeOtherAdmins.lessDetails_text)
                .withCustomReportMessage("Check that Solstice prod less details btn exist")
                .perform();



    }
    @Test(groups = {"WFAT_11954", "regression"})
    @Description(" Given that I'm a logged in Solstice user  ,When I open other admins accordion," +
            "Then  i want to Verify the Solstice MBO - 1 Product")
    private void GC02() {

        new SolsticeDashboard(driver)
                .chooseÜbersichtFromMeinKonto()
                .chooseOtherAdminAccordion();

        (new ElementActions(driver)).click(SolsticeOtherAdmins.moreDetails);

        Validations.assertThat()
                .element(driver.getDriver(), SolsticeOtherAdmins.ProdHeader)
                .text()
                .contains(SolsticeOtherAdmins.ProdHeader_text)
                .withCustomReportMessage("Check that Solstice prod header exist")
                .perform();

        Validations.assertThat()
                .element(driver.getDriver(), SolsticeOtherAdmins.ProdDes)
                .text()
                .contains(SolsticeOtherAdmins.ProdDes_text)
                .withCustomReportMessage("Check that Solstice prod Description exist")
                .perform();

        Validations.assertThat()
                .element(driver.getDriver(), SolsticeOtherAdmins.personDetails)
                .text()
                .contains(SolsticeOtherAdmins.personDetails_text)
                .withCustomReportMessage("Check that Solstice personal details btn Description exist")
                .perform();

        (new ElementActions(driver)).click(SolsticeOtherAdmins.personDetails);

        Validations.assertThat()
                .element(driver.getDriver(), SolsticeOtherAdmins.noDetails)
                .text()
                .contains(SolsticeOtherAdmins.noDetails_text)
                .withCustomReportMessage("Check that Solstice no details btn Description exist")
                .perform();


        Validations.assertThat()
                .element(driver.getDriver(), SolsticeOtherAdmins.lessDetails)
                .text()
                .contains(SolsticeOtherAdmins.lessDetails_text)
                .withCustomReportMessage("Check that Solstice prod less details btn exist")
                .perform();



    }
    @Test(groups = {"WFAT_11954", "regression"})
    @Description(" Given that I'm a logged in Solstice user  ,When I open other admins accordion," +
            "Then  i want to Verify the Solstice MBO - more than 1 Product")
    private void GC03() {

        new SolsticeDashboard(driver)
                .chooseÜbersichtFromMeinKonto()
                .chooseOtherAdminAccordion();

        (new ElementActions(driver)).click(SolsticeOtherAdmins.moreDetails);

        Validations.assertThat()
                .element(driver.getDriver(), SolsticeOtherAdmins.ProdHeader)
                .text()
                .contains(SolsticeOtherAdmins.ProdHeader_text)
                .withCustomReportMessage("Check that Solstice prod header exist")
                .perform();

        Validations.assertThat()
                .element(driver.getDriver(), SolsticeOtherAdmins.ProdDes)
                .text()
                .contains(SolsticeOtherAdmins.ProdDes_text)
                .withCustomReportMessage("Check that Solstice prod Description exist")
                .perform();

        Validations.assertThat()
                .element(driver.getDriver(), SolsticeOtherAdmins.personDetails)
                .text()
                .contains(SolsticeOtherAdmins.personDetails_text)
                .withCustomReportMessage("Check that Solstice personal details btn Description exist")
                .perform();

        (new ElementActions(driver)).click(SolsticeOtherAdmins.personDetails);

        Validations.assertThat()
                .element(driver.getDriver(), SolsticeOtherAdmins.noDetails)
                .text()
                .contains(SolsticeOtherAdmins.noDetails_text)
                .withCustomReportMessage("Check that Solstice no details btn Description exist")
                .perform();

        Validations.assertThat()
                .element(driver.getDriver(), SolsticeOtherAdmins.personDetails2)
                .text()
                .contains(SolsticeOtherAdmins.personDetails_text)
                .withCustomReportMessage("Check that Solstice personal details btn Description exist")
                .perform();

        (new ElementActions(driver)).click(SolsticeOtherAdmins.personDetails2);

        Validations.assertThat()
                .element(driver.getDriver(), SolsticeOtherAdmins.noDetails2)
                .text()
                .contains(SolsticeOtherAdmins.noDetails_text)
                .withCustomReportMessage("Check that Solstice no details btn Description exist")
                .perform();


        Validations.assertThat()
                .element(driver.getDriver(), SolsticeOtherAdmins.lessDetails)
                .text()
                .contains(SolsticeOtherAdmins.lessDetails_text)
                .withCustomReportMessage("Check that Solstice prod less details btn exist")
                .perform();



    }*/

}
